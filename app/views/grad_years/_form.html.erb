<%= form_for(grad_year) do |f| %>
  <% if grad_year.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(grad_year.errors.count, "error") %> prohibited this grad_year from being saved:</h2>

      <ul>
      <% grad_year.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :year %>
    <%= f.number_field :year %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
